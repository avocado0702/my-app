shadow$provide.module$node_modules$$mui$utils$resolveProps$resolveProps = function(global, require, module, exports) {
  function resolveProps(defaultProps, props) {
    const output = {...props};
    for (const key in defaultProps) {
      if (Object.prototype.hasOwnProperty.call(defaultProps, key)) {
        const propName = key;
        if ("components" === propName || "slots" === propName) {
          output[propName] = {...defaultProps[propName], ...output[propName]};
        } else if ("componentsProps" === propName || "slotProps" === propName) {
          const defaultSlotProps = defaultProps[propName], slotProps = props[propName];
          if (slotProps) {
            if (defaultSlotProps) {
              output[propName] = {...slotProps};
              for (const slotKey in defaultSlotProps) {
                if (Object.prototype.hasOwnProperty.call(defaultSlotProps, slotKey)) {
                  const slotPropName = slotKey;
                  output[propName][slotPropName] = resolveProps(defaultSlotProps[slotPropName], slotProps[slotPropName]);
                }
              }
            } else {
              output[propName] = slotProps;
            }
          } else {
            output[propName] = defaultSlotProps || {};
          }
        } else {
          void 0 === output[propName] && (output[propName] = defaultProps[propName]);
        }
      }
    }
    return output;
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.default = resolveProps;
};

//# sourceMappingURL=module$node_modules$$mui$utils$resolveProps$resolveProps.js.map
