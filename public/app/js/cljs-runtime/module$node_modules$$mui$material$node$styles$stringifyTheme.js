shadow$provide.module$node_modules$$mui$material$node$styles$stringifyTheme = function(global, require, module, exports) {
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.stringifyTheme = function(baseTheme = {}) {
    function serializeTheme(object) {
      const array = Object.entries(object);
      for (let index = 0; index < array.length; index++) {
        const [key, value] = array[index];
        !(0,_deepmerge.isPlainObject)(value) && "undefined" !== typeof value && "string" !== typeof value && "boolean" !== typeof value && "number" !== typeof value && !Array.isArray(value) || key.startsWith("unstable_") ? delete object[key] : (0,_deepmerge.isPlainObject)(value) && (object[key] = {...value}, serializeTheme(object[key]));
      }
    }
    baseTheme = {...baseTheme};
    serializeTheme(baseTheme);
    return `import { unstable_createBreakpoints as createBreakpoints, createTransitions } from '@mui/material/styles';

const theme = ${JSON.stringify(baseTheme, null, 2)};

theme.breakpoints = createBreakpoints(theme.breakpoints || {});
theme.transitions = createTransitions(theme.transitions || {});

export default theme;`;
  };
  var _deepmerge = require("module$node_modules$$mui$utils$deepmerge$index");
};

//# sourceMappingURL=module$node_modules$$mui$material$node$styles$stringifyTheme.js.map
